@model BlogSystem.Data.Models.Comment

@{
    ViewBag.Title = "Delete Comment";
}

<h2>@ViewBag.Title</h2>
<h3 class="text-danger">Are you sure you want to delete this comment?</h3>

        <div class="panel-group">
            <div class="panel panel-primary">
                <div data-toggle="collapse" data-target="#commentDetails" class="panel-heading">
                    <div class="panel-title">
                        <i class="glyphicon glyphicon-collapse-down"></i>
                        Comment Details
                    </div>
                </div>
                <div id="commentDetails" class="panel-collapse collapse in">
                    <div class="panel-body blog-word-wrap">
                        <ul class="list-unstyled">
                            <li>
                                <strong>Comment Id: </strong>
                                @Html.DisplayFor(model => model.Id)
                            </li>
                            <li>
                                <strong>Blog Title: </strong>
                                <a href="@Url.Action("Post", "Posts", new { area = string.Empty, id = Model.PostId })">
                                    @Html.DisplayFor(model => model.Post.Title)
                                </a>
                            </li>
                            <li>
                                <strong>User: </strong>
                                @Html.DisplayFor(model => model.User.UserName)
                            </li>
                            <li>
                                <strong>Created On: </strong>
                                @Html.DisplayFor(model => model.CreatedOn)
                            </li>
                            <li>
                                <strong>Modified On:</strong>
                                @Html.DisplayFor(model => model.ModifiedOn)
                            </li>
                        </ul>

                        <div class="panel-group">
                            <div class="panel panel-default">
                                <div data-toggle="collapse" data-target="#commentContent" class="panel-heading blog-panel-heading">
                                    <div class="panel-title">
                                        <i class="glyphicon glyphicon-collapse-down"></i>
                                        <strong>Content</strong>
                                    </div>
                                </div>
                                <div id="commentContent" class="panel-collapse collapse">
                                    <div class="panel-body">
                                        @Html.DisplayFor(model => model.Content)
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

@using (Html.BeginForm("Delete", "Comments", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <button type="submit" class="btn btn-danger">Delete</button>

    @Html.ActionLink("Cancel", "Index", "Comments", new { area = "Administration" }, new { @class = "btn btn-danger" })
}